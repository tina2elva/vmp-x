docs(vm): framed 回归确认由第 50 轮掩码引入；记录两个实验的数字与教训

  · 提交状态本地 framed 全错（protected 为垃圾值）；framed 依赖 VM_FRAME_SKEW，其第 5 参数在调用方帧里；
  · 已证掩码让客户机 RSP 低 8 字节（原设计 ≡8，掩码后 ≡0）；
  · 实验 A 删掩码 ⇒ 桩整段失效（0 passed/147 failed，vm_entry 偏移差 0x20）；实验 B 保留掩码+EXTRA 16→8 ⇒
    framed 由垃圾值变"差 a+1"（10/9、15/13、45/37），方向对未修好；两者均已回退，工作区恢复 f1c0149；
  · 教训：build/ 被 gitignore，提交说明文件要放别处；改 asm 前先整段读完再一次成patch。
