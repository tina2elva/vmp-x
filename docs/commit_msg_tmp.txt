fix(e2e): 查明 mt 间歇性失败的真身是**超时**（protected=TIMEOUT、native 跑完）—— 给重负载用例独立时限并打印耗时

  · 决定性证据（29ccddf 注解）：lenN=754 lenP=7 firstDiff=0，tailN=[... mt_many rounds=8 ok]，tailP=[TIMEOUT]；
    try1/try2 重跑都能跑完 ⇒ 卡在 30s 时限边缘的偶发，不是崩溃/竞争/损坏；
  · 解释全部旧特征：约一半概率、只在 2 核 runner、本地 16 核复现不了、偏移形态每次不一致；
  · 修法：用例支持 per-case 时限（mt_many=180s），并在日志里打印受保护那次的耗时 prot_ms；
  · 复测：e2e 147 passed 0 failed；
  · 记录：此前修掉的崩溃（越界索引/读侧越界/按页提交栈/尾调用到函数中段）都是真问题，第 64 轮那个还把覆盖推到 11/11。
