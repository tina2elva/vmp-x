fix(vm): 撤除第 49 轮 int3 断言与第 50 轮对齐掩码 —— 两者是一对，e2e 回到 147/147

  · A/B 直接跑两个 blob 揭示真相：去掩码版 CRASH 0x80000003（正是第 49 轮那条 RSP 对齐 int3 断言开火），
    有掩码版 rc=0；两者 blob 大小与 entry 偏移完全相同；
  · 全链条：掩码让客户机 RSP 变 %16==0 骗过断言（但挪了 8 字节 ⇒ framed 第 5 参数错）；去掩码则断言开火（0 passed/147 failed）；
    第 52 轮撤过断言，但随后两次 git checkout -- stub/ 又把它恢复，故反复；
  · 修复：vm_interp.c 删断言 + 四平台 vm_entry_asm.S 删掩码（恢复设计值 %16==8）；e2e 147 passed 0 failed；
  · 教训：改动看起来无害却大面积失败时，先做两个产物并排跑，再动第三行代码。
