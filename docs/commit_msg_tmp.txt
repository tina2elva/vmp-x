docs(vm): 读完整段 vm_entry 后仍无法安全移除对齐掩码 —— 记为阻塞项并写明纪律

  · 四个 asm 均为纯 ASCII、掩码仅在单行；用最小改动整体重写该段并去掉 andq 后：blob 构建成功、
    vm_entry 偏移 +0x2B20→+0x2AE0，但 e2e 0 passed/147 failed（protected 输出为空）⇒ 桩整段失效；
  · 帧大小与 VM_CTX_* 手工常量都未变，唯一可见差异是入口偏移 ⇒ 机制未明，记为阻塞项；
  · 纪律：在完成两个 blob 的完整反汇编对比前，不再尝试删除该掩码；
  · 现状：第 50 轮的 framed 回归仍在（本地 5 子用例错），工作区已恢复干净，e2e=140/7。
