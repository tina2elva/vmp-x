fix(lift): 尾部 jmp 只在目标是函数入口时按尾调用翻译 —— 修好 __pyx_pymod_create，覆盖 10/11 → 11/11

  · 根因（第 63 轮定位）：__pyx_pymod_create 的 `jmp 0x2D98` 被按"函数外=尾调用"翻成 CALLN+RET，
    但 0x2D98 是别人函数的中段/尾声、其实是本函数的续段；原生调用由解释器发出，真实栈是解释器的，
    那段按 [rsp+..] 恢复保存位必然错位 ⇒ 崩在 __Pyx_PyObject_CallMethO 区域；
  · 修法：新增 looksLikeFuncEntry（入口前通常是 0xCC/0x90/0x00 填充）——目标是填充后起点才按尾调用翻，
    否则当普通跳转翻，并让孤岛机制把该目标解码进来（孤岛收集同时接受 Jmp 目标）；
  · 验证：__pyx_pymod_create 单独保护后 import 成功（OK 55 / Hello, vmp! / 5.0）；
    e2e 147 passed 0 failed；coverage(10) 仍逐字节一致 ⇒ 无回归；
  · coverage_pyd.py 默认函数表加入 __pyx_pymod_create，KNOWN_BAD 清空 ⇒ 口径 11/11。
